fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic[email]==2.10.3
python-dotenv==1.0.1
# Used by .cursor/skills/market-map/scripts/validate_schema.py (M8 ingest pipeline). Pinned
# alongside the backend so the same venv can run both the API and the ingest scripts.
jsonschema==4.23.0
# Used by .cursor/skills/market-map/scripts/upload_logo.py to resize + WebP-encode
# project logos before uploading to the Supabase Storage `project-logos` bucket.
Pillow==11.3.0
# Used by backend/scripts/ingest_network_upgrades.py to parse YAML front-matter on every
# EIP markdown file pulled from github.com/ethereum/EIPs (M8.9 weekly worker).
python-frontmatter==1.1.0
# Used by .cursor/skills/market-map/scripts/enrich_optimistic_rollups.py (M8.10) to read
# the Perplexity-cleaned XLSX workbook stashed under
# .cursor/skills/market-map/sectors/rollup-scaling-frameworks/_source/. Will be reused by
# enrich_zk_rollups.py / enrich_l3_appchain_frameworks.py / enrich_validiums*.py.
openpyxl==3.1.5
