INSTANTLY_API_KEY=
INSTANTLY_CAMPAIGN_ID=

# Supabase (M8: Market Map data store). The anon key is the read-only public key used
# by the FastAPI routes. The service_role key is reserved for the ingest scripts under
# .cursor/skills/market-map/scripts/ and MUST NEVER be shipped to the frontend.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

ALLOWED_ORIGINS=http://localhost:3000,https://canhav.com,https://www.canhav.com
ENVIRONMENT=development
PORT=8000
