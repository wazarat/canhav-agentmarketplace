---
name: market-map/rollup-scaling-frameworks/optimistic-rollups
description: Background reference for the Optimistic Rollups subsector. Auto-loads when the agent is working under .cursor/skills/market-map/sectors/rollup-scaling-frameworks/. Not surfaced in /menu.
user-invocable: false
---

# Optimistic Rollups — Subsector reference

Sheet: `1J08OAuQ5UW4HQfoOrInTxYnoKXWqppOCRBr-1PxaKLk` tab `1623116093`

## Subsector-specific fields

Document each field that belongs in `subsector_attributes` for this subsector. Fields shared
across multiple subsectors of `rollup-scaling-frameworks` belong in the sector SKILL.md instead.

| Field key | Type | Required | Description |
|-----------|------|----------|-------------|
| _example_field_ | string | no | _description_ |

The canonical JSON Schema this list maps to lives at `schemas/subsectors/optimistic-rollups.json`.

## Column map

`schemas/subsectors/optimistic-rollups.column_map.json` maps CSV column headers to either a
universal column or a key in `subsector_attributes`. Edit it whenever the source sheet's
columns change.

## Ingest notes

- Source sheet anomalies, columns to skip, manual fixups, etc.
