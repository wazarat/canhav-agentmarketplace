---
name: market-map/advanced-compute-integration/cross-chain-compute
description: Background reference for the Cross-Chain Compute subsector. Auto-loads when the agent is working under .cursor/skills/market-map/sectors/advanced-compute-integration/. Not surfaced in /menu.
user-invocable: false
---

# Cross-Chain Compute — Subsector reference

Sheet: `1mpaWTCz9tTaKiJ1sBENEsetbRo85NX2NrCvOTbVtvZU` tab `403856203`

## Subsector-specific fields

Document each field that belongs in `subsector_attributes` for this subsector. Fields shared
across multiple subsectors of `advanced-compute-integration` belong in the sector SKILL.md instead.

| Field key | Type | Required | Description |
|-----------|------|----------|-------------|
| _example_field_ | string | no | _description_ |

The canonical JSON Schema this list maps to lives at `schemas/subsectors/cross-chain-compute.json`.

## Column map

`schemas/subsectors/cross-chain-compute.column_map.json` maps CSV column headers to either a
universal column or a key in `subsector_attributes`. Edit it whenever the source sheet's
columns change.

## Ingest notes

- Source sheet anomalies, columns to skip, manual fixups, etc.
