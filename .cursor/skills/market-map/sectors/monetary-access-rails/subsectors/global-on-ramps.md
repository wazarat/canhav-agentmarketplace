---
name: market-map/monetary-access-rails/global-on-ramps
description: Background reference for the Global On-Ramps subsector. Auto-loads when the agent is working under .cursor/skills/market-map/sectors/monetary-access-rails/. Not surfaced in /menu.
user-invocable: false
---

# Global On-Ramps — Subsector reference

Sheet: `1MyXItem529dr0NGkXmVQXS0zvzdwm-yNTQoh56LYEtI` tab `536722612`

## Subsector-specific fields

Document each field that belongs in `subsector_attributes` for this subsector. Fields shared
across multiple subsectors of `monetary-access-rails` belong in the sector SKILL.md instead.

| Field key | Type | Required | Description |
|-----------|------|----------|-------------|
| _example_field_ | string | no | _description_ |

The canonical JSON Schema this list maps to lives at `schemas/subsectors/global-on-ramps.json`.

## Column map

`schemas/subsectors/global-on-ramps.column_map.json` maps CSV column headers to either a
universal column or a key in `subsector_attributes`. Edit it whenever the source sheet's
columns change.

## Ingest notes

- Source sheet anomalies, columns to skip, manual fixups, etc.
